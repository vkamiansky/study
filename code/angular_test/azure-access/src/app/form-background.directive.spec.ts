import { FormBackgroundDirective } from './form-background.directive';

describe('FormBackgroundDirective', () => {
  it('should create an instance', () => {
    //const directive = new FormBackgroundDirective();
    //expect(directive).toBeTruthy();
  });
});
