import { HoverShowDirective } from './hover-show.directive';

describe('HoverShowDirective', () => {
  it('should create an instance', () => {
    //const directive = new HoverShowDirective();
    //expect(directive).toBeTruthy();
  });
});
